# PicoCmt 操作マニュアル

本デバイスは、Raspberry Pi Pico Wを使用した8bitレトロPC向けのCMT（カセット磁気テープ）エミュレータです。SDカード内のイメージファイル（.T88, .CMT, .CASなど）を音声として再生（ロード）したり、実機からの音声を録音（セーブ）してイメージファイルに変換しSDに保存することができます。また、WiFi経由でのFTPファイル転送にも対応しています。

## 1. ボタン構成と基本操作

本体には9つの操作ボタンがあります。

*   **[UP] / [DOWN]**: カーソルの上下移動、文字・数字の変更
*   **[LEFT] / [RIGHT]**: 階層の移動、ボーレート変更、頭出し（シーク）、カーソルの左右移動
*   **[OK]**: 決定、フォルダを開く、再生・録音の開始/一時停止
*   **[CANCEL]**: キャンセル、上の階層へ戻る
*   **[PLAY]**: 再生の開始/一時停止
*   **[REC]**: 録音の開始、WiFi設定時の保存アクション
*   **[STOP]**: 停止、アイドル画面への復帰、設定メニューへの遷移（長押しまたは連続押し）

---

## 2. ファイルブラウザ（アイドル画面）

SDカード挿入時、最初に表示される画面です。SDカード内のファイルやフォルダを選択します。

*   **移動**: **[UP] / [DOWN]** ボタンでファイルを選択します。
*   **フォルダを開く**: フォルダを選択して **[OK]** または **[RIGHT]** を押します。
*   **上の階層に戻る**: **[CANCEL]** または **[LEFT]** を押します。

### ボーレートの変更機能
ファイルリスト上で拡張子が `.CMT` または `.CAS` のファイルを選択すると、画面右にボーレート設定UIが表示されます。
*   ファイルを選択した状態で **[LEFT] / [RIGHT]** を押すとボーレートを切り替えられます。
    *   **CMTファイル**: `600` または `1200`
    *   **CASファイル**: `1200` または `2400`

---

## 3. テープの再生（PCへのロード）

PCへプログラムをロードする際の操作です。

1.  ファイルブラウザで再生したいファイルを選択し、**[OK]** または **[PLAY]** を押します。
2.  **「REC STANDBY」**(再生待機) 画面になります。
3.  PC側でロードコマンド（`LOAD` や `CLOAD` など）を実行します。
4.  本機の **[OK]** または **[PLAY]** を押すと再生（PLAYING）が開始され、テープアニメーションが動きます。
5.  再生中に **[PLAY]** を押すと一時停止（PAUSED）します。
6.  終了・中断する場合は **[STOP]** を押すとファイルブラウザに戻ります。

### 無音区間のスキップ（頭出し機能）
再生待機中、または再生中に **[LEFT] / [RIGHT]** ボタンを押すと、テープの無音区間（ブランク）を検知して前後のデータブロックへスキップ（シーク）することができます。

---

## 4. テープへの録音（PCからのセーブ）

PCから出力されるピーガー音を録音し、デジタルファイルに変換・保存します。

### 4-1. 新規録音
1.  ファイルブラウザ画面で、フォルダまたは録音非対応ファイルを選択した状態で **[REC]** を押します。
2.  自動的に `NEWRECxxx.T88`（※拡張子は設定による）というファイル名が割り当てられ、録音待機状態になります。
3.  本機の **[OK]** を押すと録音が開始されます。PC側でセーブコマンドを実行してください。
4.  保存が終わったら **[STOP]** を押します。
5.  自動的に「Converting...」画面が表示され、録音されたRAW音声データが指定のファイル形式に変換・保存されます。

### 4-2. 追記録音（アペンド）
1.  ファイルブラウザで既存の対応ファイル（`.T88`, `.CAS`）を選択し、**[REC]** を押します。
2.  録音メニューが表示されます。
    *   `Create New File` (新規作成)
    *   `Append to File` (選択したファイルに追記)
3.  **[UP] / [DOWN]** で選択し、**[OK]** を押して録音待機画面へ進みます。以降は新規録音と同じです。

*(※現在のバージョンでは RAWから T88 および CMT への変換がサポートされています)*

---

## 5. 設定メニュー（WiFi・録音フォーマット設定）

ファイルブラウザ画面で **[STOP]** を長押しすると、設定メニュー（SETTINGS）が開きます。

### 5-1. REC Format (録音フォーマットの変更)
メニューから `Rec Fmt` にカーソルを合わせ、**[LEFT] / [RIGHT]** を押すことで、新規録音時に作成されるファイルの形式を選択できます。
*   選択肢: `T88` / `CMT` / `CAS`

### 5-2. WiFi機能とFTPサーバー
Pico WのWiFi機能を利用して、SDカードを抜き差しすることなくFTP経由でファイルを転送できます。

#### WiFiの初期設定
1.  設定メニューの `WiFi Config` を選択し **[OK]** を押します。
2.  周囲のアクセスポイントをスキャンします。**[UP] / [DOWN]** で接続したいSSIDを選び、**[OK]** を押します。
3.  パスワード入力画面になります。**[UP] / [DOWN]** で文字を選択、**[RIGHT] / [LEFT]** で桁を移動し、完了したら **[REC]** を押します。
4.  **PINコード入力画面** になります。これはパスワードを暗号化して保存するための**4桁の暗証番号**です。任意の数字を設定し、**[OK]** で保存します。

#### WiFiへの接続
1.  設定メニューの `WiFi:` の項目を `ON` にします。
2.  PINコードの入力を求められるので、初期設定で決めた4桁の数字を入力して **[OK]** を押します。
3.  接続が成功すると、画面下部に `IPアドレス` とFTP接続用の `ID` / `PW` が表示されます。
4.  PCのFTPクライアントソフトに表示されたIP、ID、パスワードを入力して接続してください。

#### WiFi設定のリセット
設定メニューの `WiFi:` を **[LEFT] / [RIGHT]** ボタンで `CLEAR` に変更し、**[OK]** を押すと、保存されているSSIDとパスワード情報が安全に消去されます。

*(※注意: 再生中や録音中は処理落ちを防ぐため、WiFiおよびFTP機能は自動的に切断されます。)*